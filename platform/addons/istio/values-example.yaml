# Example values for Istio components
istiod:
  pilot:
    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        cpu: 1000m
        memory: 1Gi
ingress:
  service:
    type: LoadBalancer
