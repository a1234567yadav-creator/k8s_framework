# Flux Addon

This repository bootstraps Flux via `scripts/bootstrap-flux.sh`. For reference, you can also install Flux via Kustomize using the upstream manifests.

- Upstream manifests: https://github.com/fluxcd/flux2/tree/main/manifests
- Default namespace: `flux-system`
