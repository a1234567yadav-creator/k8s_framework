# Ingress NGINX Configuration for dev
# Resource constraints removed - Kubernetes will auto-manage
controller:
  service:
    type: LoadBalancer
  
  ingressClassResource:
    name: nginx
    enabled: true
    default: true
  
  resources: {}

  config:
    proxy-body-size: "100m"
    use-forwarded-headers: "true"
